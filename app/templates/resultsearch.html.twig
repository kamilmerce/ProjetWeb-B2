{% extends "base_auth.html.twig" %}

{% block body %}
    <h2>Résultats de la recherche</h2>
    <button onclick="window.location.href='profile.php'" class="btn btn-primary">Retour</button>
    <div style="width: 1200px; height: 500px; overflow-y: scroll; border: 1px solid #ccc; padding: 10px; margin-bottom: 20px;">
        {% if type == 'intervention' %}
            {% for result in results %}
                <p>{{ result.id }} {{ result.infos }}</p>
            {% endfor %}
        {% else %}
            {% for result in results %}
                <p>{{ result.name }} {{ result.surname }}</p>
            {% endfor %}
        {% endif %}
    </div>
    
{% endblock %}
